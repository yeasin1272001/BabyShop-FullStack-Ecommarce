const loginUser = (req, res) => {
  res.send("Login controllers is working");
};

const registerUser = (req, res) => {
  res.send("Register controllers is working");
};

export { loginUser, registerUser };
